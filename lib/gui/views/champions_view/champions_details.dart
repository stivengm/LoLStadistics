import 'package:flutter/material.dart';

class ChampionsDetail extends StatelessWidget {
  const ChampionsDetail({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}